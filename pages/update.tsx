import React, { FC } from 'react'
import Layout from '../src/components/Layout';

const UpdatePage: FC = () => {

    return (
        <Layout>
            UPDATE TRUST PAGE
        </Layout>
  );
};

export default UpdatePage
